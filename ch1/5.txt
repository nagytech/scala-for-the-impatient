10 max 2 means return the maximum of this number and that number.

(In our case, it returns 10)

max in this case is defined in the RichInt class, but is also found in other Rich numeric classes like RichDouble, RichFloat.


